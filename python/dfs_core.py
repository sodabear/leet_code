# dfs
# rightmost = {} // rightmost[3] = 7 depth at 3 when value is 7

def dfs(d):
	if d == None:
	 	return None
	rightmost[depth] = most
	dfs(d.left)
	dfs(d.right)

# Question:print out the whole tree
